[build]
  command = "npm run build"
  publish = "dist"

# SPA: todas las rutas sirven index.html (el ruteo lo maneja React en el cliente).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
